# Local only — copy to .env (gitignored). Do not commit a real host.
VITE_API_BASE_URL=

# On Vercel (Project → Settings → Environment Variables), set this server-only var
# (not VITE_). Used by api/bridge proxy. Leave VITE_API_BASE_URL empty on Vercel.
# API_BACKEND_URL=http://YOUR_HOST:8002
